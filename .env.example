# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Analytics (optional)
NEXT_PUBLIC_GA_ID=your_ga_id

# OpenAI GPT API (server-side only)
OPENAI_API_KEY=your_openai_api_key

# Perplexity API (곡 분석 레퍼런스 검색용 — 강력 권장)
# https://www.perplexity.ai/settings/api 에서 발급
# 이 키가 없으면 GPT만으로 분석하므로 조성/마디 등 팩트 정확도가 낮아질 수 있음
PERPLEXITY_API_KEY=your_perplexity_api_key

# OMR Server (optional - PDF → MusicXML 변환)
# Railway 등에 배포한 Audiveris OMR 서버 URL
OMR_SERVER_URL=https://your-omr-server.up.railway.app
